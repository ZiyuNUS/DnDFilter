# global params for diffusion model
# normalized min and max
state_stats:
  max: [128, 128, 1, 1] # [min_dx, min_dy]
  min: [-128, -128, -1, -1] # [max_dx, max_dy] 128-1


# data specific params
RED:
  metric_waypoint_spacing: 0.12